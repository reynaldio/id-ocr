# Binaries
*.exe
*.dll
*.so
*.dylib

# Test artifacts
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Editor / OS
.idea/
.vscode/
.DS_Store

# Sample ID scans are never committed; see testdata/.gitignore (the folder is
# kept, its contents ignored).
