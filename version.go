package idocr

// Version is the semantic version of the id-ocr library. It is kept in sync
// with the git tag used to publish each release (e.g. tag "v0.2.0").
//
// The library follows Semantic Versioning (https://semver.org). While the
// major version is 0 the public API may change between minor releases.
const Version = "0.2.0"
